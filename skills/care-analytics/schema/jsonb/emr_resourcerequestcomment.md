# emr_resourcerequestcomment JSONB shapes

Distilled from Pydantic API specs — strong hints, not guarantees; custom serializers can change the stored shape.

## history

- shape unknown — no spec declares this field (check serializers; default is dict)

## meta

- dict, optional — ResourceRequestCommentBaseSpec, ResourceRequestCommentCreateSpec, ResourceRequestCommentListSpec, ResourceRequestCommentRetrieveSpec
