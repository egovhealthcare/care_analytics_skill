# emr_medicationrequestprescription JSONB shapes

Distilled from Pydantic API specs — strong hints, not guarantees; custom serializers can change the stored shape.

## history

- shape unknown — no spec declares this field (check serializers; default is dict)

## meta

- dict, optional — BaseMedicationRequestPrescriptionSpec, MedicationRequestPrescriptionReadSpec, MedicationRequestPrescriptionResource, MedicationRequestPrescriptionRetrieveDetailedSpec +4
