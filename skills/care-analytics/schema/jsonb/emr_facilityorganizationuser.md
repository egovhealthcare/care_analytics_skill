# emr_facilityorganizationuser JSONB shapes

Distilled from Pydantic API specs — strong hints, not guarantees; custom serializers can change the stored shape.

## history

- shape unknown — no spec declares this field (check serializers; default is dict)

## meta

- dict, optional — FacilityOrganizationUserBaseSpec, FacilityOrganizationUserReadSpec, FacilityOrganizationUserUpdateSpec, FacilityOrganizationUserWriteSpec
